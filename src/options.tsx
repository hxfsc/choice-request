import React from "react"
import ReactDOM from "react-dom/client"
import OptionsSettings from "./components/Options/Settings"

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <OptionsSettings />
  </React.StrictMode>
)
